# 回声编辑部 PROJECT_CHARTER

> 本文件是回声编辑部的产品锚点。它不是视觉规范，也不是开发任务清单，而是回答一个问题：这个项目到底要变成什么。

---

## 0. 一句话定位

> **回声编辑部是一间 AI 编辑室，把短视频素材变成可判断、可改写、可审稿、可包装、可复盘的内容资产。**

Echo Editorial is an AI editorial room that turns short-video material into content assets that can be judged, rewritten, reviewed, packaged, and learned from.

---

## 1. 它不是一条管道

当前代码已经跑通了一条自动化链路：

```text
抖音 URL / MP4 / 手动文案
  -> 提取 / 转写
  -> AI 改写
  -> TTS 配音
  -> 视频合成
```

这条链路有价值，但它只是底层能力。

真正的编辑部不是一条线，而是一张圆桌：

```text
素材发现 -> 选题判断 -> 分配编辑角色 -> 初稿生成 -> 审稿评分
  -> 多轮修改 -> 配音/封面/字幕/视频包装 -> 发布准备 -> 数据回响 -> 风格沉淀
```

管道的本质是自动化。编辑部的本质是判断、分工、迭代和复盘。

---

## 2. 核心概念

| 概念 | 含义 |
|---|---|
| 素材 Source | 抖音链接、本地 MP4、手动文案、未来的趋势源 |
| 选题 Topic | 判断素材是否值得做、适合哪个平台、从哪个角度切入 |
| 稿 Draft | 平台文案、标题、标签、口播稿 |
| 审稿 Review | 对稿件进行评分、批注、修改建议、放行或退回 |
| 包装 Package | 配音、字幕、封面、配图、竖屏视频 |
| 模板 Template | 可复用、可分享、可 fork 的编辑方法论 |
| 回响 Echo | 发布后的数据反馈，用来优化下一轮创作 |
| 风格 Style | 用户自己的语气、节奏、偏好和历史表现 |

---

## 3. 四个编辑角色

现有界面里有四个角色元素。它们不应该只是 UI 装饰，而应该逐步变成编辑职能。

| 角色 | 编辑职能 | 产品问题 |
|---|---|---|
| 选题编辑 | 判断素材价值 | 这个视频值不值得改写？适合哪个平台？ |
| 初稿编辑 | 生成第一版稿件 | 用哪个模板、哪个角度、什么语气出稿？ |
| 审稿编辑 | 评分与修改 | 这篇稿子标题、开头、结构、口语感是否过关？ |
| 包装编辑 | 多媒体发布准备 | 配音、字幕、封面、视频是否能发？ |

角色名字可以换，视觉可以换，但这套分工应该保留。

---

## 4. 三层能力

### 第一层：内容生成管线

这是当前已有基础：

```text
提取 -> 转写 -> 改写 -> 配音 -> 视频
```

目标：让素材可以快速变成稿、声、片。

### 第二层：编辑工作流

这是近期最重要的产品升级：

```text
选题 -> 初稿 -> 审稿 -> 修改 -> 包装 -> 发布准备
```

目标：让用户感觉自己在经营一间编辑室，而不是点一个生成按钮。

### 第三层：创作者资产系统

这是长期护城河：

```text
模板 -> 历史稿件 -> 个人风格 -> 发布数据 -> 复盘反馈
```

目标：让工具逐渐理解创作者自己的表达方式，并沉淀可复用资产。

---

## 5. 真正的护城河

回声编辑部最有价值的东西不是 ffmpeg、Puppeteer 或某个 API，而是已经写进 prompt 的编辑方法论。

当前代码里的小红书 prompt 已经包含：

- CES 评分机制
- 标题公式
- 情绪钩子
- 去 AI 味规则
- 平台格式约束
- TTS 友好写法

这不是普通 prompt，而是一个主编脑内规则库的雏形。

未来要把这些规则产品化成模板系统：

- 用户可创建模板
- 模板可分享
- 模板可 fork
- 模板可评分
- 模板可形成社区资产
- 不同行业拥有不同模板：母婴、科技、财经、探店、知识科普、情感、职场

模板系统不是一个 feature，而是平台化入口。

---

## 6. 内容供应链

当前项目覆盖：

| 环节 | 状态 |
|---|---|
| 趋势发现 | 未覆盖 |
| 选题判断 | 未覆盖 |
| 内容提取 | 已覆盖 |
| AI 改写 | 已覆盖 |
| 语音生成 | 已覆盖 |
| 视频生成 | 已覆盖 |
| 多平台分发 | 未覆盖 |
| 数据反馈 | 未覆盖 |

下一步要往两端走：

- 上游：趋势发现、选题判断
- 下游：多平台发布、数据反馈、复盘

“回声”不只表示 echo，也可以表示 echolocation（回声定位）：发布后的数据回响，应该反过来优化下一次创作。

---

## 7. 技术韧性

当前依赖的风险：

| 依赖 | 风险 |
|---|---|
| 抖音反爬 | 页面结构和接口可能随时变化 |
| DeepSeek API | 涨价、限流、不可用 |
| edge-tts | 依赖微软语音服务和网络环境 |
| Pexels/Pixabay | 英文图库，对中文内容匹配有限 |

架构方向：

- 多 AI 后端路由：DeepSeek / Claude / 本地模型
- 本地 TTS 备选：调研开源中文 TTS
- 输入源多样化：RSS、微博热搜、知乎热榜、小红书趋势
- 内容原创度 / 同质化检测
- 失败降级：链接失效时切换上传 MP4 或手动粘贴
- 所有外部服务通过 adapter（适配器）接入

---

## 8. 三条战略路线

| 路线 | 终局 | 核心赌注 |
|---|---|---|
| A. 编辑 OS | 创作者的全流程内容工作台 | 多角色 AI 编辑 + 编辑方法论 |
| B. 模板生态 | 风格模板的 GitHub / Notion 模板市场 | 社区增长 + 模板资产 |
| C. 分发网络 | 多平台发布 + 数据回馈闭环 | 平台 API + 数据分析 |

启动顺序：

```text
先 A：编辑 OS
再 B：模板生态
最终 C：分发网络
```

A 离现有代码最近，也最能让用户立刻感受到“这不是普通改写工具”。

---

## 9. 对外表达原则

首页和 README 不应只展示“能生成视频”，而应展示：

1. 这是一间 AI 编辑室。
2. 它有编辑分工：选题、初稿、审稿、包装、复盘。
3. 它产出三类内容资产：稿、声、片。
4. 它会沉淀方法论：模板、历史稿件、个人风格、发布反馈。
5. 它未来会听见回响：数据反哺下一次创作。

视觉方向：

- 明亮
- 清新
- 有效率
- 有秩序
- 现代编辑部
- 创作者工作台
- 稿件批注
- 选题板
- 声音波形
- 竖屏视频
- 复盘数据

明确避免：

- 机器
- 炼金
- 赛博
- 黑夜
- 杂乱
- 大装置
- 漂浮物
- 普通办公室
- 看不出产品功能

---

## 10. 当前阶段

当前阶段不是“已经成熟的编辑 OS”，而是：

> 已经跑通内容生成管线，正在向 AI 编辑室演化。

近期优先级：

1. 工程侧：路径、测试、CI、模块拆分。
2. 产品侧：选题判断、稿件历史、多版本、审稿评分。
3. 品牌侧：统一 README、落地页、项目叙事与 hero 视觉。

